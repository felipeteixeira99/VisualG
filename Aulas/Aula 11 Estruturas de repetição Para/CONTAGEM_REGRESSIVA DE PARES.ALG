algoritmo "Contagem_regressiva de pares"
var

   cont, valor: Inteiro

inicio

   escreva("DIGITE UM VALOR: ")
   leia(valor)

   Se (valor%2=1) entao
      valor <- valor - 1
   FimSe

Para cont <- valor ate 0 passo -2 faca
escreval(cont)
Fimpara

fimalgoritmo