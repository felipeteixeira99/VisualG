algoritmo "Contador de 10 a 0"
var

   contador: inteiro

inicio

   contador <- 10
   Enquanto (contador >= 0 ) faca
      escreval(contador)
      contador <- (contador - 2)
   FimEnquanto
   escreval("Terminei de contar")

fimalgoritmo